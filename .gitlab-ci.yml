# ================================
# GitLab CI/CD Pipeline
# GYM-147 Full Stack Application
# ================================

image: docker:24.0.5

services:
  - docker:24.0.5-dind

stages:
  - lint-test
  - build
  - deploy

variables:
  DOCKER_DRIVER: overlay2
  DOCKER_TLS_CERTDIR: "/certs"

before_script:
  - docker info

# ==================== JOB 1: LINT & TEST ====================
lint-and-test:
  stage: lint-test
  image: node:20-alpine
  before_script:
    - apk add --no-cache git
    - npm install -g npm
  script:
    - npm ci
    - npm run lint || echo "No lint script found"
    - npm test --if-present || echo "No tests found"
  cache:
    key: ${CI_COMMIT_REF_SLUG}
    paths:
      - .npm/
  only:
    - main
    - develop
    - merge_requests

# ==================== JOB 2: BUILD BACKEND ====================
build-backend:
  stage: build
  image: docker:24.0.5
  before_script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
  script:
    - |
      for SERVICE in identity-service member-service schedule-service billing-service; do
        echo "Building $SERVICE..."
        docker build
          --pull
          --cache-from $CI_REGISTRY_IMAGE/$SERVICE:latest
          -t $CI_REGISTRY_IMAGE/$SERVICE:$CI_COMMIT_SHA
          -t $CI_REGISTRY_IMAGE/$SERVICE:latest
          -f services/$SERVICE/Dockerfile
          .
        docker push $CI_REGISTRY_IMAGE/$SERVICE:$CI_COMMIT_SHA
        docker push $CI_REGISTRY_IMAGE/$SERVICE:latest
      done
  artifacts:
    paths:
      - services/*/dist/
  only:
    - main
    - develop

# ==================== JOB 3: BUILD WEB ADMIN ====================
build-web-admin:
  stage: build
  image: docker:24.0.5
  before_script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
  script:
    - echo "Building web-admin..."
    - docker build
        --pull
        --cache-from $CI_REGISTRY_IMAGE/web-admin:latest
        -t $CI_REGISTRY_IMAGE/web-admin:$CI_COMMIT_SHA
        -t $CI_REGISTRY_IMAGE/web-admin:latest
        -f apps/web-admin/Dockerfile
        .
    - docker push $CI_REGISTRY_IMAGE/web-admin:$CI_COMMIT_SHA
    - docker push $CI_REGISTRY_IMAGE/web-admin:latest
  only:
    - main
    - develop

# ==================== JOB 4: BUILD GATEWAY ====================
build-gateway:
  stage: build
  image: docker:24.0.5
  before_script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
  script:
    - echo "Building gateway..."
    - docker build
        --pull
        --cache-from $CI_REGISTRY_IMAGE/gateway:latest
        -t $CI_REGISTRY_IMAGE/gateway:$CI_COMMIT_SHA
        -t $CI_REGISTRY_IMAGE/gateway:latest
        -f infrastructure/gateway/Dockerfile
        .
    - docker push $CI_REGISTRY_IMAGE/gateway:$CI_COMMIT_SHA
    - docker push $CI_REGISTRY_IMAGE/gateway:latest
  only:
    - main
    - develop

# ==================== JOB 5: DEPLOY TO VPS ====================
deploy-vps:
  stage: deploy
  image: alpine:latest
  before_script:
    - apk add --no-cache openssh-client ca-certificates
    - eval $(ssh-agent -s)
    - echo "$VPS_SSH_KEY" | tr -d '\r' | ssh-add -
    - mkdir -p ~/.ssh
    - chmod 700 ~/.ssh
    - ssh-keyscan -H $VPS_HOST >> ~/.ssh/known_hosts
  script:
    - echo "Deploying to VPS..."
    - ssh -o StrictHostKeyChecking=no $VPS_USER@$VPS_HOST "
        cd $VPS_DEPLOY_PATH &&
        docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY &&
        docker compose -f docker-compose.prod.yml pull &&
        docker compose -f docker-compose.prod.yml down &&
        docker compose -f docker-compose.prod.yml up -d &&
        docker compose -f docker-compose.prod.yml logs --tail=50 &&
        docker image prune -f
      "
  environment:
    name: production
    url: https://gym-147.example.com
  only:
    - main
  when: manual
